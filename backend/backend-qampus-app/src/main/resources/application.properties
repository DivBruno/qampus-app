spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/qampus}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
api.security.token.secret=${JWT_SECRET:my-secret-key}